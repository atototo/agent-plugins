# Claude Code delivery

Use the desktop Browser/preview or a connected Claude in Chrome integration when
available. CLI Chrome requires a working extension/login/permissions; do not assume
it is present. Otherwise use an available local browser route. Deliver the saved
HTML using the surface's working file reference or preview. Never emit another
host's Visualize token or assume Claude chat Artifacts exist in a Code terminal.
